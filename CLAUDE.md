# Qazana Strata — maintenance rules

The maintenance contract for agents **and** contributors lives in
**[AGENTS.md](AGENTS.md)** — one source, shared by Claude Code (via the import
below) and other agent tooling. Read that file before changing anything here.

@AGENTS.md

## internal-suite context (pull from the hub)
Part of the internal suite. Shared context, pulled on demand:
- Suite rules → (internal)
- This project's hub node (status, public channels, links) → (internal)
- Social/service credentials → the internal access layer (the access layer; references only, never secrets)
